# Optional: upgrade the AI assistant to Gemini (best free-tier model).
# Get a free key at https://aistudio.google.com/apikey — no credit card needed.
# Without a key the assistant uses a keyless free API, and if that is
# unreachable it falls back to the built-in offline demo assistant.
GOOGLE_GENERATIVE_AI_API_KEY=
